/**
 * Training hyperparameters. Kept in a TF.js-free module so UI (e.g. grown-up
 * mode) can display them without pulling TensorFlow into the main bundle.
 */
export const LEARNING_RATE = 0.001;
export const EPOCHS = 30;
